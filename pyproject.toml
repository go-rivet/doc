[project]
name = "Rivet"
version = "0.1.0"
description = "Rivet - Minimal Task"
site_url = "https://go-rivet.github.io/"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.43",
]
