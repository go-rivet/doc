# Variable

Variables may be scalar values or mappings. Mapping values can use:

- `sh`: A shell command whose output supplies the value.
- `ref`: A reference to another variable.
- `map`: A map-valued variable.
